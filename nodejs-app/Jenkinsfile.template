pipeline {
  agent { label 'nodejs-app' }
  options { 
    buildDiscarder(logRotator(numToKeepStr: '2'))
  }
  stages {
    stage('Test') {
      steps {
       container('node-js') {
        echo 'Hello World!'   
        sh 'node -version'
       } 
      }
    }
  }
}
